# RollDice
A Java console application that simulates rolling one or more dice. The program validates user input, generates random dice rolls, displays ASCII dice faces, and calculates the total score.
